// /negocio/leads — la lista la pinta el layout; aquí no hay panel abierto.

import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Leads · Negocio' };

export default function LeadsPage() {
  return null;
}
