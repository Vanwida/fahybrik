// Hoy cargando: el esqueleto con la forma de la bandeja (cabecera, chips, grupos
// y filas), no uno genérico.

import { HoySkeleton } from '@/components/v2/hoy/HoySkeleton';

export default function HoyLoading() {
  return <HoySkeleton />;
}
