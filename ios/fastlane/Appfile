app_identifier("pro.aistudios.vanwida.fahybrik")
apple_id(ENV["FASTLANE_USER"] || "vanwida@aistudios.pro")

# Replace once Alex is enrolled in the Apple Developer Program. The 10-char
# team ID is visible in https://developer.apple.com → Membership.
team_id(ENV["FASTLANE_TEAM_ID"] || "TBD")

# itc_team_id is only required if the Apple ID is a member of multiple App
# Store Connect teams. Solo dev: leave unset.
# itc_team_id(ENV["FASTLANE_ITC_TEAM_ID"])
